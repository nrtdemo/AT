# Alarm ticket web monitoring
For installing on CentOS

    - yum install MySQL-python
    - yum install python-pip
    - yum insatll ./google-chrome.repo
    - pip install -r requirment.txt
    
Database
    install alarm_ticket.sql